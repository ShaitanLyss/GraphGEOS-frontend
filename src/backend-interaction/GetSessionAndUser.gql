query SessionAndUser($sessionToken: String!) {
    session(sessionToken: $sessionToken) {
        id
        expires
        sessionToken
        userId
        user {
        id
        name
        email
        emailVerified
        }
    }
}